# npm グローバルパッケージ一覧
# インストール: mise exec node -- npm install -g $(grep -v '^#' packages.txt | grep -v '^$' | tr '\n' ' ')

@anthropic-ai/claude-code   # Claude Code CLI
agent-browser               # ブラウザ操作エージェント
